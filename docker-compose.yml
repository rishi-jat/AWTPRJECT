version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app/server
    environment:
      - NODE_ENV=development
